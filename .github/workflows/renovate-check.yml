name: Renovate Auto-Merge Check

on:
  pull_request:
    branches:
      - 'renovate/**'
    types: [opened, synchronize, reopened]

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run a basic command
        run: echo "Renovate check passed"